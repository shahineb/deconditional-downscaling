################################################################################
#   CONFIGURATIONS FOR SWISS ROLL EXPERIMENT WITH GP REGRESSION
################################################################################

# MODEL SPECIFICATIONS
model:
  # Name of model to use
  name: 'gp_regression'


# DATASET
dataset:
  # Number of bags
  nbags: 50

  # Mean size of sampled bags
  mean_bag_size: 100

  # Size standard deviation of sampled bags
  std_bag_size: 50

  # Number of samples to use in groundtruth manifold
  n_samples_groundtruth: 10000

  # Variance of noise to apply on individuals targets
  individuals_noise_variance: 0.05

  # Variance of noise to apply on aggregate targets
  aggregate_noise_variance: 0.50

  # Random seed for dataset generation
  seed: 42


# TRAINING
training:
  # Name of model to use
  name: 'gp_regression'

  # Learning rate
  lr: 0.05

  # Number of iterations
  n_epochs: 30


# EVALUATION
evaluation:
  # Number of samples to use for NLL computation - doesn't scale
  n_samples_nll: 2000

  # Random seed for samples selection
  seed: 42
